// AUTH constants used for storing and accessing keys in localStorage
const AUTH = {
  // Key name for storing the authentication token in localStorage
  Token_key: "token",
  // Key name for storing the user's email in localStorage
  USER_KEY: "email",
};

export default AUTH;